public class Point {
    
    double x, y;
    	
    	Point(double inc_x, double inc_y) {
    		
    		this.x = inc_x;
    		this.y = inc_y;
    		
    	}
  
	public static double angleToTurn(Point p3, Point p4) {
	
		return Math.atan(Math.abs(p3.y - p4.y)/Math.abs(p3.x - p4.x));
    
	}
    
}
